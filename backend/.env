
# Exemplo de variáveis de ambiente para desenvolvimento
# NÃO comite o arquivo .env real com segredos em repositórios públicos.

# Porta onde o servidor Express irá rodar (padrão 3001)
PORT=3001

# URI do MongoDB (ajuste conforme seu ambiente local ou container)
# Exemplo local padrão para MongoDB rodando na máquina
MONGO_URI=mongodb://localhost:27017/costura-agil

# Chave secreta usada para assinar JWTs — troque por uma string forte em produção
JWT_SECRET=segredo_super_secreto

# URL do serviço Java NFe
JAVA_NFE_SERVICE_URL=http://localhost:8082


